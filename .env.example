# Example environment variables for local development

# Generalize values for example (do NOT put real secrets here)
# Replace placeholder values with real ones in your local .env or deployment environment.

# Frontend (React)
# - Copy to frontend/.env (or set env in your dev server) and do NOT commit real secrets
REACT_APP_API_BASE_URL=http://localhost:5137

# Backend (ASP.NET Core)
# - ConnectionStrings__DefaultConnection: use a local sqlite file for dev or a connection string for production DB (Postgres/MySQL/SQLServer)
#   Example for sqlite: "Data Source=contacts.db" -> For production replace with a managed DB connection
ConnectionStrings__DefaultConnection=Data Source=contacts.db
ASPNETCORE_ENVIRONMENT=Development
# ASPNETCORE_URLS can be changed for production; do not expose dev ports publicly
ASPNETCORE_URLS=http://localhost:5137

# Optional demo auth key (for simple demo protection)
# - change to a strong value if you enable DEMO_API_KEY based protection
DEMO_API_KEY=changeme
